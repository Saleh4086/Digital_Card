DIGITAL CARD ONLY - CRM VARIABLE NAME FIX

This package uses the EXISTING Cloudflare variables already shown on your Digital Card Worker:
- CRM_OWNER_USER_ID
- SUPABASE_URL
- SUPABASE_SERVICE_ROLE_KEY

Do not change those three variables.

CRM source is forced to: Digital Business Card

Email notification remains optional. The card will save to CRM even without email variables. Email can be connected separately after CRM/source is confirmed working.

This ZIP contains only Digital Card deployment files, not the Blackstone website.
